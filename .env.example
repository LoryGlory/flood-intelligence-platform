# =============================================================================
# Environment variables — copy this file to .env.local and fill in your values.
#
#   cp .env.example packages/web/.env.local
#
# .env.local is gitignored and must NEVER be committed.
# =============================================================================

# -----------------------------------------------------------------------------
# LLM provider (optional for MVP — defaults to StubLLMProvider if not set)
# -----------------------------------------------------------------------------

# Anthropic API key — required to use the real Claude explanation agent.
# Get yours at: https://console.anthropic.com/
# Leave blank to use the deterministic stub (no API calls, works offline).
ANTHROPIC_API_KEY=

# Optional: override the default model (claude-sonnet-4-6)
# ANTHROPIC_MODEL=claude-sonnet-4-6

# -----------------------------------------------------------------------------
# CI / GitHub Actions secrets — NOT for .env.local
# These are set in your GitHub repo under:
#   Settings → Secrets and variables → Actions → New repository secret
# -----------------------------------------------------------------------------

# SONAR_TOKEN  — required for the SonarCloud step in ci.yml.
#   1. Go to https://sonarcloud.io → sign in with GitHub
#   2. My Account → Security → Generate Token
#   3. Add as repo secret named SONAR_TOKEN